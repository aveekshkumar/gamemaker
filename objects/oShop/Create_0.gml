/// --- oShop Create Event ---
// Don't load here — oGlobal already loaded it!
// Just use the global values that oGlobal already set

// Define shop items
shop_items = [
    ["Shield", 20, "Survive one hit (consumed once)"],
    ["Aura",   50, "Glowing trail upgrade"],
    ["Speed",  75, "Run faster & scroll quicker"]
];

shop_index = 0;
shop_color = make_color_rgb(30, 30, 40);