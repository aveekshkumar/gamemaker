// Random movement
hspeed = random_range(-2, 2);
vspeed = random_range(-3, -1);
gravity = 0.1;

// Visual settings
image_blend = c_white;
image_alpha = 1;
fade_speed = 0.05;

// Depth in front of everything
depth = -50;

// Destroy timer
life_timer = 60;